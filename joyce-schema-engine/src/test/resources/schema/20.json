{
  "$schema": "https://joyce.sourcesense.com/v1/schema",
  "metadata": {},
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "required": true,
      "value": "$.email",
      "apply": [{
        "handler": "transform",
        "args": "uppercase"
      }]
    }
  }
}
