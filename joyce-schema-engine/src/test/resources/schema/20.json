{
  "$schema": "https://joyce.sourcesense.com/v1/schema",
  "$metadata": {
    "message_key": "$.id"
  },
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "required": true,
      "$path": "$.email",
      "$transform": "uppercase"
    }
  }
}
